# Synthetic Ignore-Boundary Fixture

This fixture contains no secrets. During disposable QA, create clearly synthetic ignored files such as `.env` and `secrets/fake-key.pem` after initializing the copied fixture as a Git repository. The adoption flow must not open or quote them.
