# Small Service Context

A synthetic single-component JavaScript service used to test root coding maps.

The public greeting function lives in `src/greeting.js`. Tests are authoritative for supported examples.
