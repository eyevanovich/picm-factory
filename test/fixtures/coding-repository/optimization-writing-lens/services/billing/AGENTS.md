# Billing Instructions

Read this document only for a billing task after `CONTEXT.md` routes you here.

Before modifying billing code, inspect the local implementation and tests.

## Customer data

Do not access customer production data. This repeats the root rule intentionally because an agent may begin directly in `services/billing`.

## Production changes

Get human approval before changing production billing behavior.
