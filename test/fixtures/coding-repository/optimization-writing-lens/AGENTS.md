# Agent Instructions

`AGENTS.md` is the canonical root guide for every coding task.

## Start every task

Before choosing task-specific guidance, read `CONTEXT.md`.

## Code changes

- Start at `src/main.js`.
- Run `npm test`.
- A code change is complete when `npm test` passes.

## Customer data

Do not access customer production data.
