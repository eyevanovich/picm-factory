# Writing-Lens QA Fixture

This synthetic disposable workspace exercises approved `/picm-optimize` documentation changes. It contains a canonical root guide, a duplicated compatibility guide, an explicit task map, local billing and release instructions, and a generated read-only deployment guide. The fixture distinguishes true duplication from deliberately repeated local safety and approval rules.
