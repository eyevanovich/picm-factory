# Shared Package Context

## Purpose and ownership
Owns reusable greeting formatting and must not import application code.

## Read first
- `src/format.js`

## Verification
Tests live under `test/`; the root `package.json` owns the aggregate test command.
