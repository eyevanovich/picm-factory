# Synthetic Monorepo Instructions

Read `CONTEXT-MAP.md` before choosing an application or package. Then read the selected boundary's `CONTEXT.md` when one is listed.

Do not read Git-ignored paths. Keep `.picm/` outside normal coding context.
