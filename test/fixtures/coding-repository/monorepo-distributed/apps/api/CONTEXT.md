# API Context

## Purpose and ownership
Owns the public greeting request handler.

## Read first
- `src/server.js`
- `../../packages/shared/src/format.js`

## Verification
Tests live under `test/`; the root `package.json` owns the aggregate test command.
