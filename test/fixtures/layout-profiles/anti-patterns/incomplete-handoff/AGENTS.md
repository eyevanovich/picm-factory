# Synthetic Service Workspace

This fixture contains synthetic, non-sensitive data only.

- Use `intake/` to understand a public service request.
- Use `delivery/` only after reading the reviewed handoff under `handoffs/`.
