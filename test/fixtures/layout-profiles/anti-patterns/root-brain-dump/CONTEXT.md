# Fixture Context

Synthetic, non-sensitive anti-pattern fixture. The intended primary finding is an oversized root `AGENTS.md` that mixes routing, history, payload references, workflow rules, and stale decisions.
