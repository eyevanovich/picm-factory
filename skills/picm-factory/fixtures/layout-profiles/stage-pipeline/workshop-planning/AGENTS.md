# Workshop Planning Agent Guide

This workspace uses nested stages under `stages/`.
Read root `CONTEXT.md`, then the current stage context.

Do not skip ahead: discovery decisions should inform design, and design decisions should inform follow-up.
