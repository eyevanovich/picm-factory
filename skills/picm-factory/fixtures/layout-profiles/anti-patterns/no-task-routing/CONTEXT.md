# Editorial Workspace Context

The workspace supports two distinct tasks:

- Research public facts using `research/CONTEXT.md`.
- Prepare approved copy using `publishing/CONTEXT.md`.

The root routing file does not map either task to its local context.
