# Volunteer Program Agent Guide

Route volunteer program work by role folder:

- `recruiting/` for outreach and signup context.
- `scheduling/` for shift planning and availability.
- `shift-support/` for day-of support guidance.
- `handoffs/` for passing work between roles.

Use local role `CONTEXT.md` files for role-specific guidance.
